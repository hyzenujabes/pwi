<div class="container">
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="card" style="width: 18rem;">
                <img src="..." class="card-img-top" alt="no hay imagen">
                <div class="card-body">
                        <h5 class="card-title">{{usuario.username}}</h5>
                        <p class="card-text">Mail: {{usuario.mail}}</p>
                        <p class="card-text">Telefono: {{usuario.telefono}}</p>
                        <p class="card-text">Fecha de creacion: {{usuario.ts_create}}</p>
                        <a href="/usuarios/edit" class="btn btn-primary">Editar</a>
                </div>
            </div>
        </div>
    </div>
</div>